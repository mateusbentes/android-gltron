namespace GltronMobileGame.Video
{
    public static class ModelLoader
    {
        // Placeholder loader to be extended later (OBJ/FBX/etc.)
        public static SimpleModel LoadLightcycle()
        {
            // For now we just return a placeholder model wrapper
            return new SimpleModel();
        }
    }
}
